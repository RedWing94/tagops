# Terraform — state, secrets, lock files, binaries
**/.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
terraform.tfvars
terraform/terraform.exe
terraform/LICENSE.txt

# GCP service account keys — never commit these
*.json.key
*-credentials.json
service-account*.json

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# dbt — generated artifacts
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/package-lock.yml

# Secrets
.env
*.pem
